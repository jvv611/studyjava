重点是

- HashMap：put 过程、扩容机制、为什么初始容量是 2 的幂次方、链表转红黑树的条件，这些几乎每次都会问，而且很容易被追问到细节。
- ConcurrentHashMap：JDK 1.7 的分段锁和 JDK 1.8 的 CAS + synchronized 方案的区别，是并发面试里的高频考点。
- ArrayList 的线程安全问题：为什么不安全、具体会出现哪些问题、有哪些替代方案，这个看起来简单但答起来很容易流于表面。
- equals 和 hashCode 的关系：这两个方法为什么要配套重写，放到 HashMap 和 HashSet 里会有什么影响，基础但经常被忽略。

## 数组和集合的区别

数组只能存储固定长度，可以存储对象和基本数据类型，而集合只能存储对象，并且可以根据存储数量动态变化长度，数组可以直接访问元素，集合只能通过迭代器或者其他方法访问元素

- ArrayList：动态数组，实现了 List 接口，支持动态增长。
- LinkedList：双向链表，也实现了 List 接口，支持快速的插入和删除操作。
- HashMap：基于哈希表的 Map 实现，存储键值对，通过键快速查找值。
- HashSet：基于 HashMap 实现的 Set 集合，用于存储唯一元素。
- TreeMap：基于红黑树实现的有序 Map 集合，可以按照键的顺序进行排序。
- LinkedHashMap：基于哈希表和双向链表实现的 Map 集合，保持插入顺序或访问顺序。
- PriorityQueue：优先队列，可以按照比较器或元素的自然顺序进行排序。

List 是有序的 Collection，使用此接口能够精确的控制每个元素的插入位置，用户能根据索引访问 List 中元素。常用的实现 List 的类有 LinkedList，ArrayList
ArrayList 是容量可变的非线程安全列表，其底层使用数组实现。当几何扩容时，会创建更大的数组，并把原数组复制到新数组。ArrayList 支持对元素的快速随机访问，但插入与删除速度很慢。
LinkedList 本质是一个双向链表，与 ArrayList 相比，其插入和删除速度更快，但随机访问速度更慢。

Set 不允许存在重复的元素，与 List 不同，set 中的元素是无序的。常用的实现有 HashSet，LinkedHashSet 和 TreeSet。

- HashSet 通过 HashMap 实现，HashMap 的 Key 即 HashSet 存储的元素，所有 Key 都是用相同的 Value，一个名为 PRESENT 的 Object 类型常量。使用 Key 保证元素唯一性，但不保证有序性。由于 HashSet 是 HashMap 实现的，因此线程不安全。
- LinkedHashSet 继承自 HashSet，通过 LinkedHashMap 实现，使用双向链表维护元素插入顺序。
- TreeSet 通过 TreeMap 实现的，添加元素到集合时按照比较规则将其插入合适的位置，保证插入后的集合仍然有序。

Map 是一个键值对集合，存储键、值和之间的映射。Key 无序，唯一；value 不要求有序，允许重复。Map 没有继承于 Collection 接口，从 Map 集合中检索元素时，只要给出键对象，就会返回对应的值对象。主要实现有 TreeMap、HashMap、HashTable、LinkedHashMap、ConcurrentHashMap

- HashMap：JDK1.8 之前 HashMap 由数组 + 链表组成的，数组是 HashMap 的主体，链表则是主要为了解决哈希冲突而存在的（“拉链法” 解决冲突），JDK1.8 以后在解决哈希冲突时有了较大的变化，当链表长度大于阈值（默认为 8）时，将链表转化为红黑树，以减少搜索时间
- LinkedHashMap：LinkedHashMap 继承自 HashMap，所以它的底层仍然是基于拉链式散列结构即由数组和链表或红黑树组成。另外，LinkedHashMap 在上面结构的基础上，增加了一条双向链表，使得上面的结构可以保持键值对的插入顺序。同时通过对链表进行相应的操作，实现了访问顺序相关逻辑。
- HashTable：数组 + 链表组成的，数组是 HashTable 的主体，链表则是主要为了解决哈希冲突而存在的
- TreeMap：红黑树（自平衡的排序二叉树）
- ConcurrentHashMap：Node 数组 + 链表 + 红黑树实现，线程安全的（jdk1.8 以前 Segment 锁，1.8 以后 volatile + CAS 或者 synchronized）

**线程安全的集合**

有两个线程安全的类一个是Vector，另一个是Hashtable。他们的内部方法会经过synchronized锁，实现线程安全。同步开销大。Vector 内部是使用对象数组来保存数据，可以根据需要自动的增加容量，当数组已满时，会创建新的数组，并拷贝原有数组数据。table上锁是对Table 对象，不支持 null 键和值。

一、并发 Map 核心对比

**ConcurrentHashMap 与 HashTable**

- **锁机制差异**：二者均为线程安全集合，但锁粒度与实现天差地别。
    - **HashTable**：采用**全局对象锁**，直接对整个 `table` 数组对象加锁，所有操作（put/get）竞争同一把锁，并发场景下锁竞争极严重，性能低下。
    - **ConcurrentHashMap（JDK1.7）**：引入**分段锁（Segment 机制）**，将大数组拆分为多个 Segment 小数组，每个 Segment 独立持锁，不同 Segment 的并发操作互不阻塞，并发度显著提升。
    - **ConcurrentHashMap（JDK1.8）**：废弃分段锁，改用 **CAS + synchronized** 锁，仅对链表/红黑树的**头节点/根节点**加锁，锁粒度进一步细化，并发性能达到最优。
- **结构与特性**：
    - **HashTable**：底层为**数组+链表**，不支持 null 键和 null 值，容量满时按固定比例扩容，同步开销极大。
    - **ConcurrentHashMap**：底层为 **数组+链表/红黑树**，支持 null 键（仅 Key）、null 值（JDK1.8 后优化），链表长度≥8 时转为红黑树，查询效率更高。
- **关联记忆**：HashTable 是“全仓锁”的老旧实现，ConcurrentHashMap 是“精准锁”的高并发替代方案，彻底解决 HashTable 的性能瓶颈。

二、并发 Set 核心对比

**ConcurrentSkipListSet 与 CopyOnWriteArraySet**

- **底层实现与有序性**：
    - **ConcurrentSkipListSet**：基于 **ConcurrentSkipListMap（跳表）** 实现，**天然有序**，通过跳表的多层索引结构，在对数时间内完成增删查，适合高并发下的有序去重场景。
    - **CopyOnWriteArraySet**：基于 **CopyOnWriteArrayList（写时复制动态数组）** 实现，**无序**，利用 List 的去重方法保证元素唯一性，底层无哈希表结构。
- **线程安全机制**：
    - **ConcurrentSkipListSet**：依赖跳表的无锁并发逻辑，CAS 操作保证原子性，读写无阻塞，并发性能极强。
    - **CopyOnWriteArraySet**：核心为**写时复制**，写操作加锁并复制新数组，读操作无锁，适合读多写少的并发场景。
- **关联记忆**：ConcurrentSkipListSet 对应非并发的 TreeSet（跳表有序替代红黑树），CopyOnWriteArraySet 对应非并发的 HashSet（动态数组替代哈希表），二者分别适配“高并发有序”与“读多写少无序”场景。

三、并发 List 核心对比

**CopyOnWriteArrayList 与 Vector**

- **线程安全实现**：
    - **Vector**：采用**全局 synchronized 锁**，所有方法（add/get/remove）共享同一把对象锁，无论读写都加锁，同步开销巨大，并发性能差。
    - **CopyOnWriteArrayList**：核心为**写时复制**，写操作加锁并复制原数组生成新数组，读操作无锁直接访问，实现读写分离，读性能远优于 Vector。
- **底层与适用场景**：
    - **Vector**：底层为**动态对象数组**，容量满时按 2 倍扩容，适合低并发、简单同步场景。
    - **CopyOnWriteArrayList**：底层为**可复制动态数组**，适合读多写少的高并发场景（如配置缓存、白名单），写操作有一定性能开销。
- **关联记忆**：Vector 是传统同步 List，并发性能拉胯；CopyOnWriteArrayList 是高并发优化版 List，通过牺牲写性能换高并发读性能。

四、并发 Queue 核心对比

**ConcurrentLinkedQueue 与 BlockingQueue**

- **核心定位与机制**：
    - **ConcurrentLinkedQueue**：**无锁非阻塞并发队列**，基于链表实现，通过 CAS 原子操作实现入队/出队，全程无锁，专为高并发场景设计，性能远超阻塞队列。
    - **BlockingQueue**：**有锁阻塞队列**，核心为**阻塞等待机制**，队列为空时消费者阻塞，队列满时生产者阻塞，不追求高并发性能，而是简化多线程数据共享（生产者-消费者模型）。
- **适用场景**：
    - **ConcurrentLinkedQueue**：适合高并发下的异步任务、消息传递场景，追求极致吞吐量。
    - **BlockingQueue**：适合多线程协作场景，如线程池任务队列，通过阻塞机制控制线程节奏。
- **关联记忆**：ConcurrentLinkedQueue 是“无锁高性能”队列，BlockingQueue 是“有锁易协作”队列，二者分别适配高并发异步与多线程协作的不同需求。

五、并发 Deque 核心对比

**ConcurrentLinkedDeque 与 LinkedBlockingDeque**

- **锁机制与并发性**：
    - **ConcurrentLinkedDeque**：**无锁非阻塞双端队列**，基于链表实现，通过 CAS 实现双向增删查，多线程并发操作无锁，并发性能极高。
    - **LinkedBlockingDeque**：**有锁阻塞双端队列**，采用独占锁（读写不分离），同一时间仅允许一个线程操作，线程安全但并发性能一般。
- **底层与特性**：
    - **ConcurrentLinkedDeque**：无界、无锁，支持双向操作，适合多线程同时访问的高并发双端场景。
    - **LinkedBlockingDeque**：有界（默认容量 Integer.MAX_VALUE）、有锁，支持双向阻塞操作，适合需要双端同步协作的场景。
- **关联记忆**：ConcurrentLinkedDeque 是“无锁高性能双端队列”，LinkedBlockingDeque 是“有锁易协作双端队列”，二者分别对应非阻塞与阻塞双端并发场景。

**collection和collections区别**

Collection是Java集合框架中的一个接口，它是所有集合类的基础接口，定义了一组通用的操作和方法，如添加、删除、遍历等，用于操作和管理一组对象。

Collections（注意有一个s）是Java提供的一个工具类，位于java.util包中。它提供了一系列静态方法，用于对集合进行操作和算法。Collections类中的方法包括排序、查找、替换、反转、随机化等等。

**遍历集合方法**

1.for循环

2.for-each

3.Iterator 迭代器

4.ListIterator 列表迭代器

5.使用 forEach 方法

6.stream api方法

## List

非线程安全的有ArrayList和LinkedList，一个查找方便一个插入方便

线程安全的有vector和CopyOnWriteArrayList

vector与ArrayList类似，大多数方法是同步的，多线程可以保证数据一致性，不过两者的调整逻辑有所区别，Vector 在扩容时会提高 1 倍，而 ArrayList 则是增加 50%。都是在原数组满了的情况下生成新数组装载原数组

CopyOnWriteArrayList：修改数组会创建一个新的数组，读操作在原数组上进行，写操作在新数组上进行，实现读写分离，适合读操作多写操作少的并发场景

!image.png

**list可以一边遍历一边修改元素吗**

使用for循环可以对遍历到的元素进行就该只要不超出下标范围。

用for-each时不建议修改因为下标未知，出错不容易调试，而且会破坏迭代器的内部状态。

使用迭代器遍历时，需要使用remove移除元素，修改元素需要迭代器的的 `set`方法而不是list的set方法。

对于线程安全的list集合如CopyOnWriteArrayList，可以遍历时修改，因为修改是在新副本上进行，但是可能会读取到旧数据

**如何快速删除指定下标元素**

arrylist和LinkedList均可以使用remove方法移除指定元素，对于arrylist他会直接删除指定下标的元素，然后将后续元素前移若不是在队尾时间复杂度o(n)。对于LinkedList他会遍历找到下标元素，修改其指针指向来删除元素时间复杂度o(1)。

CopyOnWriteArrayList也可以使用remove函数，是直接在他的副本上修改，并且由于其并发的特性，可以边修改边读取

**Arraylist和LinkedList的区别，哪个集合是线程安全的?（总结）**

ArrayList和LinkedList都是Java中常见的集合类，它们都实现了List接口。

- **底层数据结构不同**：ArrayList 使用动态数组实现，通过索引可以快速定位到元素。LinkedList 使用双向链表实现，每个节点都存储了元素本身以及指向前一个和后一个节点的指针，通过节点之间的指针关联来访问和操作元素。
- **插入和删除操作的效率不同**：ArrayList 在尾部进行插入和删除操作时效率较高，因为不需要移动其他元素；但如果是在中间或开头插入、删除，就需要移动后面的所有元素，效率会比较低。LinkedList 在头部和尾部进行插入、删除操作时效率很高，只需要调整节点的指针即可；但如果是在中间位置操作，需要先从头或尾遍历链表找到目标位置，时间复杂度也是 O(n)，不过找到位置后只需要调整指针，不需要像 ArrayList 那样移动大量元素，所以在某些特定场景下还是有优势的，而且 LinkedList 实现了 Deque 接口，还可以当作双端队列、栈来使用。
- **随机访问的效率不同**：ArrayList 支持通过索引直接快速访问元素，时间复杂度为 O(1)。LinkedList 不支持随机访问，想要获取某个位置的元素，必须从头节点或尾节点开始逐个遍历，时间复杂度为 O(n)。
- **空间占用**：ArrayList 在创建时会分配一段连续的内存空间，虽然会有一定的容量浪费（比如实际元素没装满数组），但只需要存储元素本身。LinkedList 每个节点除了存储元素，还需要额外存储两个指针（指向前一个和后一个节点），所以在存储相同数量元素的情况下，LinkedList 的空间占用通常会比 ArrayList 更大一些。
- **使用场景**：ArrayList 更适合需要频繁随机访问元素，或者主要在尾部进行插入、删除操作的场景。LinkedList 更适合需要频繁在头部或尾部进行插入、删除操作，或者需要作为双端队列、栈使用的场景；如果是通过迭代器直接操作已知位置的节点，在中间插入、删除时也能发挥它调整指针快的优势。
- **线程安全**：这两个集合都不是线程安全的，如果在多线程环境下使用，需要自己加锁保证线程安全，或者使用线程安全的 List 集合，比如 Vector、Collections.synchronizedList() 包装的 List，或者 CopyOnWriteArrayList。

**arraylist和vector的区别**

首先vector是线程安全的，基本所有方法都被synchronized 修饰，多线程下操作不需要处理同步问题而arraylist需要处理数据不一致问题。也正因为所有方法都被synchronized 修饰，单线程开销比arraylist开销大，单线程的性能比arraylist低下。另外vector一次扩容是乘以2，而arraylist是乘以1.5

想要arraylist线程安全只有三种方法，如图

!image.png

**为什么arraylist线程不安全**

主要是因为arraylist的操作不是原子性操作导致的，在多线程情况下，两个线程竞争同一个add操作，当线程1add完毕以后还未执行list的size++操作，线程2也add完了就会导致部分下标中值为null

当线程1执行add发现不用扩容，但是还未放入元素。线程2也发现不用扩容，两个线程在之后放入元素，导致长处数组容量了，就导致了数组越界

线程1和线程2add完毕元素后，执行size++操作拿到了一样的size值，导致有一次没有加上，导致add和size值不一致

!image.png

**CopyOnWriteArrayList如何保证线程安全**

CopyOnWriteArrayList底层也是通过一个数组保存数据，使用volatile修饰数组保证修改数组其他线程可以感知到。

在写入操作时，加了一把互斥锁ReentrantLock以保证线程安全。在加入新元素时候会复制一份原数组，并让原来的数组长度加1组成新数组，然后在新数组放入元素，之后将旧数组的引用指向新数组。这样写数据前后都是读取的有效数据

**为什么List<>里面填基本数据类型为什么会报错？**

因为泛型之初就是为了引用类型，泛型在编译后会成为object，object只能接受引用类型。另外集合中使用引用类型可以实现装箱拆箱，方便操作基本数据类型。

list和数组的转换

list转换为数组可以使用toArray()无参方法，此方法只能返回Objetc类型

或者是toArray()有参，可以直接返回指定参数的数组类型

数组转list

核心是用`Arrays.asList()`

`Arrays.asList()`返回的 List 不是 ArrayList，而是 Arrays 的内部类，不支持添加 / 删除操作，想修改就套一层 ArrayList。

基本类型数组（int []、long []）直接用`Arrays.asList()`会把整个数组当成一个元素，必须手动装箱或用 Stream 流转换为包装类（Integer）的 List。

java集合.pdf

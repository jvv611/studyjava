!image.png

## 前端部分

!image.png

可以通过clas属性，id属性或者块属性确定要生效的css部分

!image.png

!image.png

<br>可以换行，<p>是段标签

!image.png

!image.png

!image.png

!image.png

!image.png

表格

!image.png

!image.png

!image.png

!image.png

!image.png

!image.png

## js

!image.png

!image.png

!image.png

!image.png

函数方法

!image.png

json格式属性必须有双引号

!image.png

json是数据载体传递前后端数据用来

!image.png

!image.png

BOM

!image.png

DOM

!image.png

!image.png

事件监听

!image.png

!image.png

vue

!image.png

!image.png

!image.png

!image.png

!image.png

!image.png

ajax 从服务器端获取数据

!image.png

!image.png

!image.png

!image.png

前端工程化

!image.png

Elment

!image.png

# 后端部分

## maven

用来管理依赖

!image.png

!image.png

!image.png

!image.png

!image.png

!image.png

!image.png

!image.png

!image.png

!image.png

## web springboot

!image.png

!image.png

!image.png

## http协议

!image.png

!image.png

!image.png

!image.png

!image.png

!image.png

!image.png

!image.png

## 封装HTTP协议的服务器

## Tomcat

也成为web容器或者servelet容器

!image.png

!image.png

## 初级程序解析

!image.png

!image.png

!image.png

## 请求响应

!image.png

httpservletrequest接受数据并解析

httpservletresponse设置需要相应的数据

!image.png

!image.png

controller需要获取请求参数以及设置响应数据

!image.png

!image.png

!image.png

!image.png

springboot方式测试get请求

当形参对不上时，使用@requestparam

!image.png

!image.png

需要传递参数多时需要封装为对象

!image.png

!image.png

!image.png

数组参数传递

!image.png

!image.png

日期参数传递

!image.png

json传递

利用post传递

!image.png

!image.png

!image.png

路径参数

!image.png

多个路径参数传递

!image.png

!image.png

@responsebody

!image.png

!image.png

不一致难以管理，统一管理

!image.png

!image.png

## 分层解耦

!image.png

!image.png

单一职责原则，每一部分只做一个职责

!image.png

提升可维护性和复用性

三层架构controller和service耦合，service和dao耦合，要高内聚低耦合

!image.png

不再new对象再上一层，而是将下一层存储再容器，上一层在容器中查找即可

**控制反转（IOC）**以及**依赖注入（DI）**实现

!image.png

1.删除controller的new service对象以及service删除new dao对象

@component表示当前类交给ioc管理，成为ioc容器中的bean，在service层以及dao写

2.@autowired完成依赖注入，controller以及service写

!image.png

!image.png

!image.png

不能归于三层使用另外三个注解

!image.png

可以在idea中查看bean名字，也可以使用value指定名字

!image.png

!image.png

!image.png

!image.png

可以手动指定要扫描哪个包，但是会覆盖默认扫描两者都要手动指定，建议默认扫描

!image.png

多个bean不知道选哪个

@PRIMAYR可以设置优先级，选定那个优先生效

@qualifier直接指定用哪一个

第三个与上一个一样，只是第二个用按照类型注入，第三个是按照名字注入

!image.png

## MyBatis

通过java程序操作数据库

!image.png

可以实现基础增删改查，以及动态sql

!image.png

引入MyBatis依赖以及MySql依赖

配置sql提示

!image.png

!image.png

!image.png

datasource自动存储连接池操作，不需要释放资源、

数据库连接池

!image.png

**durid连接池，springboot默认hikari连接池**

!image.png

**lombok**

!image.png

## MyBatis基础操作

!image.png

删除操作及测试

!image.png

!image.png

!image.png

!image.png

**sql语句执行流程 先检查缓存再顺序执行**

!image.png

!image.png

蓝色框为预编译，问号为占位符，会把语句存到缓存，把问号替换为id，至于要编译一次句子，之后从内存取出即可，不用再次编译

预编译也可以防止sql注入，使用问号占位符替代了字符串拼接避免了sql注入问题

MyBatis中#{}即为预编译sql语句

!image.png

**插入数据及测试**

!image.png

!image.png

主键返回

!image.png

## 编辑操作

1.查询并展示出来数据

2.修改数据

!image.png

!image.png

**查询操作**

!image.png

!image.png

MyBatis名字一致才会自动封装

!image.png

解决方法1：起别名给不同的字段起别名即可，别名与对象字段名一直

!image.png

解决方法2：使用@result方法，几个字段写几个需要字段

!image.png

解决方法3：打开MyBatis自动映射驼峰命名再propertise文件中

条件查询及测试

!image.png

!image.png

可以利用concat连接字符串以及？占位替代上方方式

!image.png

!image.png

!image.png

以上是通过注解配置sql语句，以下是基于映射xml文件配置sql语句

## 文件配置sql

需要一一对应

!image.png

简单的增删改查可以使用注解，复杂sql语句需要使用xml文件性能更好

## 动态sql

!image.png

!image.png

再MyBatis中的xml文件使用

!image.png

此时会有问题，第一个条件为null时候，第二语句and会报错，多了and词

!image.png

使用<where>标签可以动态判断是否要and，都不满足不会生成where语句

作用1自动生成where关键字

作用2判断是否需要and

!image.png

**更新**

之前的update会导致未更新数据即没有传参的数据为null

满足有数据更新，无数据不更新

再mapper中写的sql方法按住alt+回车自动在xml生成sql语句

再xml文件中sql语句太长可以ctrl+alt+L自动美化

!image.png

此时也可能会前面不成立导致后面sql语句出错，此时使用set标签即可

!image.png

会自动去除逗号以及对更新标签包裹

**foreach标签循环遍历元素，用来批量删除**

遍历传递过来的参数以数组形式存储加上前括号以及后括号以及逗号拼接为数组

!image.png

collection为遍历数组名，直接用参数数组名即可，item为间隔符

!image.png

**优化**

查询所有字段不建议使用*，而是列举出所有片段

xml文件有时候会有很多重复片段，此时使用sql标签提取重复片段，include再需要使用地方引用即可

!image.png

!image.png

**动态sql总结**

!image.png

## 案例

rest规范

[]()

[]()

统一result返回结果

[]()

开发流程

[]()

!image.png

分页插件优化分页查询

!image.png

使用时候需要引入依赖

!image.png

!image.png

分页条件查询

!image.png

动态删除，删除一个或者多个

!image.png

新增员工

!image.png

## 文件上传

前端上传

!image.png

后端接受文件

!image.png

上传的文件不存储会直接消失

!image.png

使用上述方法获取文件名，反复获取时候会导致覆盖之前文件，需要构造唯一文件名

!image.png

使用uuid保证名字不重复

默认最大为1mb，通过配置更改

!image.png

存储再本地，1.前端不能直接访问 2.内存需求大，磁盘损坏后果大

常用方法

!image.png

## 阿里云oss

!image.png

!image.png

集成前端后端

!image.png

修改信息首先需要查询到需要修改的员工id，返回给前端，然后根据前端传来的数据进行修改，即写为两个方法一个查询一个更新，查询前面已经介绍了，下面只有修改的代码

!image.png

## spring-boot配置文件

!image.png

变为

!image.png

文件格式

!image.png

!image.png

推荐使用yml配置

!image.png

yml定义数据元素

!image.png

使用注解繁琐

!image.png

用来标注前缀，免除多次@

!image.png

然后创建队形，获取对应属性

!image.png

需要引入依赖

!image.png

需要很多注入以及注入一个的区别

!image.png

## 登录校验

!image.png

此处登录后复制页面地址，不登陆直接进入该页需要更改此模式，想要访问需要重新登录

**登录校验**

没有统一拦截时候，需要对每一步操作进行登陆验证，登陆了就进行该操作，未登录则报错返回前端

统一验证可以简化操作

!image.png

每次登录会把登录标记存储，下次进去取出验证是否登录，登录就放行到对应操作

由于http是无状态的，不会存储上一次的页面状态所以需要会话追踪，看是否是同一个会话的数据开启数据共享

!image.png

cookie

!image.png

session基于cookie实现

!image.png

令牌技术

!image.png

**jwt令牌**

封装原始json

!image.png

!image.png

使用

!image.png

使用jwt的登录

!image.png

jwt传达到前端后，传回还需要拦截验证，即统一拦截并校验

Filter 将登录校验逻辑封装在filter中

!image.png

**快速入门**

!image.png

!image.png

!image.png

过滤器链

!image.png

优先执行顺序

!image.png

**拦截器intercepoer** 

!image.png

!image.png

!image.png

拦截哪些资源

!image.png

过滤器与拦截器关系

!image.png

## 异常处理

!image.png

异常走全局异常处理器

!image.png

## 事务管理

!image.png

指定所有异常回滚

!image.png

事务传播

!image.png

## AOP

!image.png

实现方法

!image.png

!image.png

aop的实际应用

!image.png

通知类型

!image.png

!image.png

抽取出切入点表达式，只需要改动一处就可以如果要更改切入点

!image.png

改为public其他类也可以使用

!image.png

通知顺序，多个通知执行统一切入点按照何种顺序进行

!image.png

与切面类类名有关系

!image.png

## 切入点表达式

!image.png

!image.png

问号可以省略

语法

!image.png

使用 || 运算符同时制定两个切入点表达式

!image.png

也可以使用annotation直接写切入点表达式，基于注解实现

先后先定义一个注解

!image.png

!image.png

会在有@MyLog的方法上施行

连接点

!image.png

around需要使用

ProceedingJoinPoint其他则使用JoinPoint

## spring原理

spring配置文件优先级properties大于yml大于yaml，yml是主流使用

!image.png

命令行参数大于java属性

!image.png

Bean管理

!image.png

实现方式

!image.png

可以更改bean的作用域，再@Scope（）中使用

!image.png

第三方Bean

!image.png

可以通过Value和Name指定bean的名称

springboot原理

起步依赖

主语要引入一个依赖

!image.png

自动配置

!image.png

!image.png

此方式扫描大面积包繁琐

!image.png

实现

!image.png

方式三 最方便

!image.png

由第三方提供注解引入依赖

!image.png

自动配置原理

!image.png

!image.png

!image.png

框架

!image.png

!image.png

!image.png

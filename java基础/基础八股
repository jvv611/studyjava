## java是**面向对象**，**平台无关注**，**自动内存回收**的语言

1.java优势在于oop（面向对象）扩展性强，生态多。劣势在于运行速度较慢

2.平台无关注使得java可以编译一次形成字节码（.class文件）而非机器码，然后就可以在安装了对应版本的jvm平台上运行，将字节码翻译为机器码，即可运行。不同平台上字节码相同，但是机器码不同。

3.jvm，jdk，jre关系

!image.png

简单来说

jvm是java的虚拟机，提供了运行程序的环境，将字节码1翻译为机器码，提供了内存管理、垃圾回收、安全性的功能。另外只要是编译后生成java字节码的语言都一可以使用jvm转换为机器码

jre是java程序运行的最小的一个必须环境，不包含开发工具

jdk是包含了运行环境以及开发环境的开发工具包，包含了开发工具，编译器，调试器

## 编译型语言和解释型语言区别

编译型语言在执行前会编译出字节码或机器码，生成可执行文件。执行时直接运行编译后代码，速度快，跨平台性差

解释型语言是有解释器动态解释并执行代码，跨平台好，速度慢

## 值传递和引用传递的区别

java中值传递和引用传递区别很小。

对于基本数据类型，使用值传递，传递的是实际值的副本，修改副本不影响原变量值

引用传递本质仍然是值传递，对于对象传递的是对象的副本，不是对象本身。如果使用副本修改原对象内部会影响原对象（如`obj.name = "Bob";`就是修改内部），如果修改副本指向，则不会影响(如`obj = new Person("Charlie");`)。

简单来说java所有对象都是值传递。

## 八种数据类型

byte，short，int，long，char，boolean，double，float

按字节数由1，2，4，8为

1：byte，boolean

2：short，char

4：int，float

8：long，double

乘以8即为位数

八种类型，除了char是Character，int是Integer，其他都是首位大写。

数据转换有四种方式

1：自动类型转换：目标类型大于原类型会自己转换如int→long

2：强制转换：目标类型小于原类型就需要强转，但是会发生数据溢出或丢失如long→int

3：字符串转换：java提供将**字符串**数据转换为其他数据类型方法，使用包装类型，如Integer.paresInt()将**字符串**目标换为int

4：数值间转换：java提供数值类型之间的转换方法，如int变为字符串，反之亦然，通过包装类实现，如Character，Integer

注意小的数据类型换为大的时候是安全的，也试默认会转换的，但是大的换为小的会强制缩减位数，导致数据溢出高位丢失。或者精度损失，如float换为double，double换为int都会损失精度。

在需要精确计算的时候不能使用double来进行计算，因为在某些情况下其不能精确表示一个小数，因为二进制只能够表示能够用1/(2^n)的和的任意组合，0.1就不能精确表示需要使用**bigDecimal，例如电商中的价格计算。**

**对象的转换，**继承的对象向上转换是安全的，向下转换需要手动强转

```java
class Animal {}
class Dog extends Animal {}

Dog dog = new Dog();
Animal animal = dog; // 自动向上转型
/////////////////////////////////////////////////////////////////////
Animal animal = new Animal();
Dog dog = (Dog) animal; // 运行时抛出ClassCastException
```

原因是Java 的对象在运行时会记录其真实类型，当进行向下转型时，Java 会检查对象的实际类型是否与目标类型兼容。如果不兼容，就会抛出`ClassCastException。`

解决方式是需要使用 `instanceof` 检查：

`if (animal instanceof Dog) {
    Dog dog = (Dog) animal; // 只有确认animal是Dog的实例时才进行转型
}`

**装箱（Boxing）和拆箱（Unboxing）**是将基本数据类型和对应的包装类之间进行转换的过程。只在赋值时以及方法调用时发生自动装箱。

自动装箱会对性能有一些影响

包装类用处

1：泛型中必须使用包装类，因为可以方便的使用一些方法

2：转换基本类型和包装类，以及基本数据类型互相转换

3：集合中不能存储基本数据类型，只能存储包装类。

**Integer相比int有什么优点？**

int是Java中的原始数据类型，而Integer是int的包装类。

Integer和 int 的区别：

- 基本类型和引用类型：首先，int是一种基本数据类型，而Integer是一种引用类型。基本数据类型是Java中最基本的数据类型，它们是预定义的，不需要实例化就可以使用。而**引用类型则需要通过实例化对象**来使用。这意味着，**使用int来存储一个整数时，不需要任何额外的内存分配**，而使用Integer时，必须为对象分配内存。在性能方面，基本数据类型的操作通常比相应的引用类型快。
- 自动装箱和拆箱：其次，Integer作为int的包装类，它可以实现自动装箱和拆箱。自动装箱是指将基本类型转化为相应的包装类类型，而自动拆箱则是将包装类类型转化为相应的基本类型。这使得Java程序员更加方便地进行数据类型转换。例如，当我们需要将int类型的值赋给Integer变量时，Java可以自动地将int类型转换为Integer类型。同样地，当我们需要将Integer类型的值赋给int变量时，Java可以自动地将Integer类型转换为int类型。
- 空指针异常：另外，**int变量可以直接赋值为0，而Integer变量必须通过实例化对象来赋值**。如果对一个未经初始化的Integer变量进行操作，就会出现空指针异常。这是因为它被赋予了null值，而null值是无法进行自动拆箱的。
- **基本类型数据在读写效率方面，要比包装类高效**。除此之外，在64位JVM上，在开启引用压缩的情况下，**一个Integer对象占用16个字节的内存空间，而一个int类型数据只占用4字节的内存空间，前者对空间的占用是后者的4倍。**也就是说，不管是读写效率，还是存储效率，基本类型都比包装类高效。

## 面向对象

一种将现实世界事物抽象为对象的方法，对象具有属性方法，利用对象之间的交互实现程序功能主要有以下性质

1.封装：对象的属性方法对外隐藏实现细节，只能通过接口和外界交互，提高安全性

1. 继承:继承是一种可以使得子类自动共享父类数据结构和方法的机制。它是代码复用的重要手段。
2. 多态:多态是指允许不同类的对象对同一消息作出不同响应。即同一个接口，使用不同的实例而执行不同操作。多态性可以分为编译时多态（重载）和运行时多态（重写）。它使得程序具有良好的灵活性和扩展性。

多态分为两种重载和重写

**重载**是一个类中具有不同的同名方法但参数不同，根据传入参数选择不同实现方式

**重写**是子类根据父类同名方法提供的新的实现方式，系统会自动调用合适的方法根据实际类型

在另外两个方面也体现了多态

接口：一个类可以继承多个接口，多个类可以继承一个接口，并通过接口调用这些类方法实现不同的结果。

上下转型：可以将子类转换为父类这是向上转型。通过这种方式，可以在运行时期采用不同的子类实现。 向下转型是将父类引用转回其子类类型，但在执行前需要确认引用实际指向的对象类型以避免 `ClassCastException`。

抽象类与普通类

1：抽象类不能实例化，只能继承，普通类可以实例化。

2：都可以被继承，同时实现多个接口

3：普通类可以被其他类继承和使用，而抽象类一般用于作为基类，被其他类继承和扩展使用。

抽象类和接口区别

1.抽象类用于**描述类的共同行为，可以有成员变量、构造方法和具体方法**。而接口是**定义行为规范可以多实现，只能有常量和抽象方法（Java 9 以后可以有默认方法和静态方法，私有方法）。**

2.实现方式不同，接口是implements实现，抽象类是abstract继承

3.接口默认时限为public static final，所有成员方法都是public，抽象类中成员变量默认default，可在子类中被重新定义，也可被重新赋值；抽象方法被abstract修饰，不能被private、static、synchronized和native等修饰，必须以分号结尾，不带花括号。

4.抽象类可以包含实例变量和静态变量，而接口只能包含常量（即静态常量）。

抽象类由于需要继承不能加final修饰，他们是互斥的。并且抽象列不能实例化即不能new出来，并且接口不能new不需要构造方法

静态变量和静态方法

它们都属于类而非对象，由static声明

静态变量：

1.**共享性**：所有该类的实例共享同一个静态变量。如果一个实例修改了静态变量的值，其他实例也会看到这个更改。

2.**初始化**：静态变量在类被加载时初始化，只会对其进行一次分配内存。

3.**访问方式**：静态变量可以直接通过类名访问，也可以通过实例访问，但推荐使用类名。

静态方法

1.**无实例依赖**：静态方法可以在没有创建类实例的情况下调用。对于静态方法来说，不能直接访问非静态的成员变量或方法，因为静态方法没有上下文的实例。

2.**访问静态成员**：静态方法可以直接调用其他静态变量和静态方法，但不能直接访问非静态成员。

3.**多态性**：静态方法不支持重写（Override），但可以被隐藏（Hide）。

一些非静态内部类和静态内部类的八股

## 关键字

final主要是修饰类，方法和变量，被修饰的这些不能再被更改，继承，重写。

static主要是用于修饰内部成员和内部类，让被修饰的成员和内部类与类有关而不是与实例有关

1.修饰的变量属于整个类，所有实例可以共享整个变量，内存中仅有一个整个static变量的副本

2.静态方法不能直接访问非静态变量和方法，但可以访问静态成员

3.静态代码块再类加载时执行一次，多个静态代码块按照顺序执行

4.修饰内部类不能直接访问外部非静态成员，可以利用外部实例访问

## 深浅拷贝

浅拷贝：只复制对象本身以及其内部的值类型字段。浅拷贝是创建一个新对象，将源对象的字段值复制到新对象去。源对象的引用类型字段复制到新对象中，**两个引用指向同一个引用对象。**

深拷贝：**会将对象内部的所有引用类型字段复制，而不是共享引用。**

深拷贝实现的三种方法

1.实现 Cloneable 接口并重写 clone() 方法

2.使用序列化和反序列化

3.手动递归复制

## 泛型

泛型允许在定义类，接口，方法的时候使用一个或者多个类型参数，在调用这些方法时才指定为具体类型。（定义泛型还要指定返回类型如下还有double）

```java
private static <T extends Number> double add(T a, T b) {
    System.out.println(a + "+" + b + "=" + (a.doubleValue() + b.doubleValue()));
    return a.doubleValue() + b.doubleValue();
}
```

主要是为了**避免在运行时出现类型转换以及适配更通用的方法**

## 对象

对象有四种创建方式

1.new

2.利用反射得到类的class，然后**newInstance()或**利用反射和**Constructor的newInstance()**

3.clone()，深拷贝

4.反序列化，要序列化和反序列必须实现`java.io.Serializable` 接口。

5.工厂模式这是一种设计模式，不直接使用 `new`而是通过一个方法来返回对象实例。`getInstance()`、`valueOf()` 等都是常见的工厂方法。

[]()

判断合适回收new出来的对象有三种方法

1.引用计数器：当引用计数器为0，表示不再被引用就回收

2.可达性分析算法：当一个对象不存在对象之间的链路可以到达他就回收

3.终结器：垃圾回收器会在回收对象前调用`finalize()`方法。

想要获取私有对象可以使用**get方法**或者使用**反射修改可访问值为true访问。**

## 反射

在运行的时候可以获取任何一个类的所有属性以及方法，任何一个对象都可以调用这个类的任意一个方法和属性。主要用来加载配置文件以及数据库配置

可以用来

1.运行时修改类访问信息，如包名，类名，字段等

2.动态创建对象，利用newinstance()实现

3.动态调用方法，通过Method类的invoke()方法实现

4.修改访问和字段值，即使是私有的

## 注解

注解是一种特殊的接口

有源码级注解（仅存在于源码中，编译后不会保留），类文件级注解（保留在 `.class` 文件中，但运行时不可见），运行时注解（保留在 `.class` 文件中，并且可以通过反射在运行时访问）

注解的解析是利用反射实现JVM 在加载类时会解析 `.class` 文件中的注解信息，并将其存储在内存中，供反射机制使用。

[]()

注解可以放在类上，方法上，字段上

除了这三种作用域，Java还提供了其他一些注解作用域，例如构造函数作用域和局部变量作用域。这些注解作用域可以用来对构造函数和局部变量进行描述和注释。

## 异常

Java 的异常体系主要基于 `Throwable` 及其子类。`Throwable` 有两个重要的子类：`Error` 和`Exception`，它们分别代表了不同类型的异常情况。
error：运行环境错误，程序无法处理

exception：程序可以处理的异常情况，分为非运行时异常，编译时必须捕获。以及运行时异常，程序逻辑错误导致

try-catch：处理和捕获异常
throw：手动抛出异常
throws关键字：用于在方法声明中声明可能抛出的异常类型。
finally块：用于定义无论是否发生异常都会执行的代码块。通常用于释放资源，确保资源的正确关闭。

| 对比项 | throw | throws |
| --- | --- | --- |
| **作用** | **手动抛出**一个具体的异常对象 | **声明**方法可能抛出哪些异常 |
| **位置** | 写在**方法内部**，执行逻辑里 | 写在**方法声明**上，参数列表后 |
| **使用次数** | 方法内**只能用一次**，抛一个对象 | 可同时声明**多个异常**，逗号分隔 |
| **后跟内容** | 跟**异常对象**（new 出来的） | 跟**异常类名** |
| **效果** | 程序立刻跳到 catch / 终止 | 告诉调用者：我这里可能出问题 |

[]()

当程序是运行时错误以及error时候不用throws，或者捕获了错误在程序内部，并处理了也不需要throws

## object

==和equals的区别

简单来说==比较地址，equal比较内容

在基本数据类型中，==就是直接比较数值是否相等，而对于对象==比较的是两个对象是否是指向同一个地址（java中string是引用类型）

equal默认也是比较地址和==一样，但是重写之后大多数就是去比较实际存储内容是否相同了

[]()

[]()

hashcode和equals

对于重写 `equals` 方法的类，通常也需要重写 `hashCode` 方法，并且需要遵循以下规定：
**一致性**：如果两个对象使用 `equals` 方法比较结果为`true`，那么它们的 `hashCode` 值必须相同。也就是说，如果 `obj1.equals(obj2)` 返回 `true`，那么`obj1.hashCode()` 必须等于 `obj2.hashCode()`。

**非一致性**：如果两个对象的 `hashCode` 值相同，它们使用 `equals` 方法比较的结果不一定为 `true`。即
`obj1.hashCode() == obj2.hashCode()` 时，`obj1.equals(obj2)` 可能为 `false`，这种情况称为哈希冲突。

[]()

| 特性 | String | StringBuilder | StringBuffer |
| --- | --- | --- | --- |
| **不可变性** | 不可变（`final` 修饰字符数组） | 可变 | 可变 |
| **线程安全** | 安全（因不可变，无并发修改问题） | 不安全（无同步锁） | 安全（方法加 `synchronized` 同步锁） |
| **性能** | 低（频繁修改会创建大量新对象） | 高（单线程无锁开销） | 中（多线程安全但有锁开销） |
| **适用场景** | 少量静态字符串、不频繁修改的场景 | 单线程下频繁字符串拼接 | 多线程下频繁字符串拼接 |

## java8新特性

| 特性名称 | 核心描述 | 示例或说明 |
| --- | --- | --- |
| **Lambda 表达式** | 简化匿名内部类，支持函数式编程 | `(a, b) -> a + b`，用于替代匿名类实现接口 |
| **函数式接口** | 仅含一个抽象方法的接口，可通过 `@FunctionalInterface` 标记 | `Runnable`、`Comparator`，或自定义接口：`@FunctionalInterface interface MyFunc { void run(); }` |
| **Stream API** | 提供链式操作处理集合数据，支持并行处理 | `list.stream().filter(x -> x > 0).collect(Collectors.toList())` |
| **Optional 类** | 封装可能为 `null` 的对象，减少空指针异常 | `Optional.ofNullable(value).orElse("default")` |
| **方法引用** | 简化 Lambda 表达式，直接引用现有方法 | `System.out::println`，等价于 `x -> System.out.println(x)` |
| **接口的默认方法与静态方法** | 接口可定义默认实现和静态方法，增强扩展性 | `interface A { default void print() { System.out.println("默认方法"); } }` |
| **并行数组排序** | 使用多线程加速数组排序 | `Arrays.parallelSort(array)` |
| **重复注解** | 允许同一位置多次使用相同注解 | `@Repeatable` 注解配合容器注解使用 |
| **类型注解** | 注解可应用于更多位置（如泛型、异常等） | `List<@NonNull String> list` |
| **CompletableFuture** | 增强异步编程能力，支持链式调用和组合操作 | `CompletableFuture.supplyAsync(() -> "result").thenAccept(System.out::println)` |

Lambda 表达式它是一种简洁的语法，用于创建匿名函数，主要用于简化函数式接口，会增加调试困难。

stram它提供了一种高效且易于使用的数据处理方式，特别适合集合对象的操作，如过滤、映射、排序等。

[]()

[]()

并行流（ParallelStream）就是将源数据分为多个子流对象进行多线程操作，然后将处理的结果再汇总为一个流对象，底层是使用通用的 fork/join 池来实现，即将一个任务拆分成多个“小任务”并行计算，再把多个“小任务”的结果合并成总的计算结果

[]()

## 序列化

[]()

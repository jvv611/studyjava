## 入门

### 入门案例

用来快速开发mybatis的程序，提高开发程序

!image.png

在配置完sprinbgbootSpring Boot 后配置

1.添加mybatis-plus的依赖，以及druid的依赖

!image.png

2.配置数据库

!image.png

3.做数据层所需要的功能

4.做dao层

mybatisplus写法如下，继承了basemapper并写了实体类是什么，提供了一些常用的sql语句

!image.png

5.测试

!image.png

### 简介

!image.png

!image.png

### 标准CRUD开发

mybatisplus提供了很全面的方法

!image.png

直接写入测试即可，已经提供了很多方法

特别是修改方法，默认没有值的时候不会去更改数据库的其他字段

!image.png

对于实体类引入lombok依赖和再ide中加入lombok插件可以使用注解标记出构造函数，get，set方法

### 标准分页查询

使用selectpage来分页查询

!image.png

查询第一页，每页2条数据

此时没有拦截器，这样的情况下会导致sql语句退化为slecat all，有了拦截器后会拦截sql方法去进行功能强化，再sql后面加上limit来实现分页查询

实现拦截器需要如下实现

!image.png

!image.png

## 条件查询

配置logbak简化日志输出

!image.png

并在配置文件配置

!image.png

### 条件查询的三种方式

wrapper用来封装查询的条件，以此实现条件查询

!image.png

实现不同的wrapper，进行crud

lt为小于

!image.png

此时容易错误输出字段，使用lambda格式实现放置错误

!image.png

对方式二的简化为，使用lambdaquerywrapper替代wrapper和lambda

!image.png

多条件格式的时候设置为，一直向后连接条件即可

!image.png

或者和and关系为使用or（）

!image.png

### 条件查询null值的处理

1.新建专门使用的查询对象，限制数值和日期的而上下限，继承实体类的属性，并且有新属性描述数值的上下限

!image.png

不具有条件的时候封装sql语句为由null的语句会导致sql错误

!image.png

使用另外的lt方式，判定是否为null才进行拼接

!image.png

!image.png

!image.png

### 查询投影

使用如下方式，实现选择看哪些字段

!image.png

不适用lmbda格式写法如下

!image.png

统计数据实现方式，需要使用selectmap，查询字段数只能使用该方法

!image.png

分组方法如下

!image.png

### 查询条件设置

1.等匹配

下面有些需要空判断，eq为判断相等，查询一个使用selectone

实际业务还需要md加密

!image.png

2.范围查询

!image.png

3.模糊匹配

!image.png

!image.png

!image.png

具体更多的条件去官网看

!image.png

### 字段映射与表名映射

使用@TableFiled（value）修改不匹配的字段

!image.png

编码中添加了数据库没有的属性使用@TableFiled（exist = false）

!image.png

对于传递私密数据的时候比如密码，使用select = false表示不参与查询

!image.png

表名和数据库名不同使用@TableName

!image.png

## 增删改

!image.png

为了适应上面，自增策略为

!image.png

有多种自增策略

!image.png

这是数据库自增策略

其他的需要数据库关闭自增有些，比如input为指定id，不需要自增

这文件里表示了生成策略

!image.png

为雪花算法，生成一个id

!image.png

简化注解的数量使用全局配置同一个策略

!image.png

这个等价于注解处代码

!image.png

或

自动拼接前面匹配数据库名字

!image.png

!image.png

#### 雪花算法

序列号为生成顺序

!image.png

### 多记录操作

使用deletebatchids

!image.png

也有批量查询

!image.png

支持一次操作多个数据

### 逻辑删除

有些业务数据不应该删除

!image.png

可以添加字段表示是否删除如0为删除，1为有效

!image.png

1.数据库里添加字段，有默认值此处0表示未删除，1表示删除

!image.png

2.实体类里添加字段，并添加注解

!image.png

进行查询的时候会自动带上where条件过滤掉逻辑删除的

!image.png

需要查询所有数据包括逻辑删除的则需要自己写查询sql语句了

可以不写注解

!image.png

转而在全局配置文件中进行，去掉麻烦，所有地方的delete字段会自动为逻辑删除

!image.png

## 乐观锁

修改操作的时候的问题

以下方法适用于小型企业开发

1.设置version字段，默认未1的值

!image.png

2.在实体类添加乐观锁，@Version

!image.png

在sql中需要对version + 1，并且未加1的version等于这次更改前的version

3.添加拦截器继续上述算法操作

有自带的拦截器

!image.png

测试如下

!image.png

注释部分和代码部分等价

!image.png

测试并发下的乐观锁

!image.png

最后数据库未jock aaa，第二个不满足version的要求

!image.png

## 代码生成器

主要导入的模块不同点是在下划线处

!image.png

可以从数据库读取需要的信息，除了灰色的标记字段所需要的类型处

!image.png

!image.png

步骤如下

1.创建代码生成器类，使用如下代码

读取数据库源来生成

!image.png

2.运行该文件会生成文件，但是文件夹位置和文件名字可能不正确

使用如下配置来配置

!image.png

所需要的包名配置如下

!image.png

还有一些策略配置，用来不同条件生成

!image.png

生成的service也提供了很多的service方法但是不常用，通常自己写

!image.png

需要把userInfo封装到User去，作为userInfo对象

但是现在直接查询会导致没有值

!image.png

因此需要使用resultmap

!image.png

具体是以下这样

!image.png

需要两个resultMap，对应user以及userInfo的对应关系

!image.png

一对一是使用association封装映射关系

一对多查询查询

!image.png

使用集合来装order

!image.png

!image.png

添加OrderMap的映射

!image.png

!image.png

一对多是使用Collection封装映射关系

多对多

!image.png

分开两个进行一对多，相互封装对面的对象类里去在pojo里和映射文件里

!image.png

可以从多的两个端点处进行查询

从另一个角度查询

!image.png

新的mapper接口

!image.png

写对应的映射文件

!image.png

级联查询可以开启自动，驼峰名与字段名字对应

一对一级联查询

每选取一个关系自动展示其他关系

如选了四川省 出现成都市 广元市 巴中市等

不同于inner join是先查询了原有内容再查询其他内容

!image.png

生成两个sql映射文件的语句，并使用asocation关联，select选取下一个查询句子，和传递的参数

!image.png

还是需要写私有其他对象进行对应

!image.png

一对多的级联查询

collection表示一对多操作

!image.png

!image.png

级联查询懒加载

fetchType=”eager“表示一起查询，lazy表示未使用时不会加载

!image.png

!image.png

一级缓存

!image.png

每一个sqlsession就是一个一级缓存，可以在一个连接查询中反复使用，Mapper存储了二级缓存

一级缓存缓存的数据会直接打印而不要查询数据库了。sqlsession的修改，添加，删除，修改，提交，关闭会导致一级缓存过期

clearCache可以手动清除缓存

二级缓存，访问一次mapper会存储在二级缓存里，多个sqlsession可以共享

!image.png

需要使用二级缓存需要实现Seriazable接口，只要用到二级缓存的类都需要序列化，统一开启所有映射文件二级缓存，单独的映射也开启，两个都开启了才能使用

!image.png

mybatis注解开发方式

直接在方法上进行注解写sql语句

!image.png

写了注解就不要有映射文件了

!image.png

注解对应级联

!image.png

mybatis的符号表示

!image.png

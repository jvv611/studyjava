手写mybatis框架没看

!image.png

!image.png

jdbc不足

1.代码繁琐 2.修改扩展程序需要更改源码

mybatis使用反射机制实现对jdbc对象的get和set

## orm思想 对象关系映射

!image.png

sql表记录与java对象相互转换

!image.png

第一个mybatis文件

!image.png

!image.png

配置xml文件

!image.png

全类名以及id未配置暂时，CarMapper.xml

!image.png

后续步骤

!image.png

!image.png

insert传参是与XxxMapper.xml文件的所要执行的sql语句的id

!image.png

sqlSession不支持自动提交，需要手动提交，但是会在后台使用掉一个主键递增数字

!image.png

!image.png

一个数据库对应一个sqlsessionfactory对象即可

resource是从resource文件夹的根目录下执行

!image.png

获取sqlsessionfactory方法

!image.png

mapper路径填写

!image.png

但是还是建议放在resource文件夹下加载

## mybatis事务管理机制

!image.png

!image.png

!image.png

!image.png

true需要手动提交

!image.png

## junit 单元测试

测试模板

!image.png

## 日志文件集成

!image.png

!image.png

集成了第三方组件后，不用在MyBatis-config.xml中配置seeting标签来指定日志，会自动查找合适方式输出日志。未集成第三方则需要配置才会输出日志

## 工具类编写

!image.png

## CRUD

**插入**

!image.png

第一个属性传递sql语句id，第二个传递对象属性具体值

!image.png

!image.png

插入对象封装到pojo’中

!image.png

测试类

!image.png

sql中的#{}里写入pojo类的属性名

!image.png

mybatis通过get方法获得属性值，由get方式才可以获得数据，实际#{}中写的是getxxx（）方法名但是省略了get

**删除**

sql语句

!image.png

!image.png

**更新**

!image.png

!image.png

**查找指定数据**

查找由返回值需要指定封装查找道德数据的类型是什么 result就是这个作用

!image.png

!image.png

!image.png

这条sql语句会导致属性名与有下划线的sql字段名不同不能赋值，例如carNum和car_num，可以使用全字段查询而不是*，以及as解决

!image.png

**查询所有数据**

!image.png

!image.png

返回List集合单数童谣需要指定返回数据类型，list<>中可以写Object也可以强转Car

namespace的作用 用来方式sql的id冲突

!image.png

当多个mapper.xml文件有相同的几个id方法时需要，指出引用哪个文件的方法，使用namespace.方法名定义。

!image.png

完整写法是namespace.id

## mybatis标签

**多环境**

!image.png

这是多个环境

!image.png

一般一个环境对应一个数据库，一个数据库对应一个sqlsessionfactory对象

enviroments使用default指定默认使用什么环境创建sqlsessionfactory对象，与enviroment的id相同

指定获取sqlseesionfactory方法

!image.png

transaction的type配置事务管理器

!image.png

mybatis提供了事务管理器接口一个jdbc一个mannage，选择哪个底层就实例化谁

datasource被称为数据源，为程序提供Connection对象，所有提供该对象的程序都称为数据源

可以自定义数据源组件，需要实现接口

!image.png

对于不同的datasource的type需要配置不同的属性

!image.png

将第三方连接池例如druid配置在tomcat中只需要在mybatis配置上下文信息

!image.png

!image.png

mybatis子啊带连接池配置，下面举出一些属性配置的例子

!image.png

properties配置

!image.png

将上图配置好，然后映射到下图属性中

!image.png

但是建议配置在propertise文件中

在配置中出现resource一定是从resource文件根目录下查询，在mybatis用resource配置可以直接查找到properties文件

!image.png

## 手写mybatis框架

准备配置文件

!image.png

注释输出，修改代码得到enviromnt标签

!image.png

正则表达式替换属性为问号

!image.png

事务控制在service层完成

sqlsession作用域

!image.png

## javassisit

!image.png

## mybatis技巧

#{}和${}区别  #{}使用preparestament预编译，再用？占位符传值

${}直接拼接值，存在sql注入问题

!image.png

sql语句关键字只能使用${}，要传递值需要使用#{}避免sql注入

当然拼接语句使用${}

批量删除

!image.png

别名机制

sql的配置文件mapper中对resultype可以起别名，不区分大小写，namespqce不能写别名

!image.png

!image.png

其中可以省略别名，默认为简写，即末尾最后一个词例如car或Car或cAr，不区分大小写

配置技巧

!image.png

接口与对应mapper文件同级别，且名字相同

!image.png

获取自动生成的主键

!image.png

第一个参数是自动获取主键，第二个是将获取参数封装到对象哪个属性

!image.png

## mybatis处理参数

简单类型

!image.png

!image.png

非简单类型，map集合传值

!image.png

map占位符是key，pojo占位符是属性名

!image.png

多参数

!image.png

mybatis底层自动创造一个map集合存储参数，需要写arg0，arg1以此类推或者param1，param2类推

!image.png

!image.png

!image.png

但是这样太麻烦可以注解指定名字

!image.png

将arg0标替换为你指定的名字，但是param1和param2可以用

## 查询

mapper接口层

!image.png

maper的xml文件

!image.png

如果只用于一个对对象接受，单数查询到多个数据会有异常，必须多个接受使用List集合接受

如果没有合适java对象接受，可以使用map集合接受

!image.png

会使用属性名作为key，接受值为value

也可以使用多个map集合 使用集合了map的list集合

!image.png

!image.png

返回大map集合

!image.png

!image.png

## 结果映射

!image.png

列colum和property是相同的可以省略，可以指定类型

!image.png

自动映射

!image.png

必须要下划线分割对应驼峰命名

## 动态sql

if标签

test属性配置为true if中sql语句会拼接

!image.png

maper配置

!image.png

传值三个不为空即可查询，都为空或几个为空可以加上where后加上1=1恒成立查询

但是where标签可以更好处理

!image.png

!image.png

!image.png

where只能去掉前面的and后面的and不能去除

trim标签

!image.png

!image.png

set标签

!image.png

!image.png

!image.png

动态生成set，自动去除，以实现传值为null则不更新

choose标签 otherwise标签以及when标签

!image.png

实现

!image.png

!image.png

至少有一个传值，全部为空则会走otherwise且传值为null

foreach标签

批量删除

!image.png

!image.png

!image.png

或是使用or删除

!image.png

!image.png

批量插入

!image.png

!image.png

sql标签和include标签

!image.png

将sql语句存入标签，使用include声明即可使用

## 高级映射

!image.png

## 多对一映射

在学生对象中使用private Clazz clazz关联班级类，使用主表关联副标=表

!image.png

第一种方式级联属性映射

实现映射文件

!image.png

第二种方式 一条sql预计 association标签

!image.png

!image.png

第三种方式 两条sql分步查询

!image.png

!image.png

!image.png

先执行sql语句然后执行retultmap的步骤

第二步

!image.png

!image.png

将第一步查询出的cid传递给第二步

!image.png

添加column，将cid传递

!image.png

以及确定传递位置

!image.png

再次执行班级信息表的查询语句，最后返回已经设置的映射的信息

## 延迟加载

!image.png

不用的时候不查询

!image.png

直接在多对一的第三种分部查询使用可以，默认不开启。

!image.png

这种方式支队当前关联sql语句起作用

!image.png

这种方式全局有用，开发中建议开启，对于不想开启的可以配置  fetchType=”eager”

## 一对多映射

!image.png

第一种方式 coolection

!image.png

mapper文件

!image.png

第二种方式 分步查询

先写出接口和配置文件，查询班级信息

!image.png

!image.png

第二步 在sqlid处写sql语句，要引用stu表的sql语句

第二步的代码

!image.png

!image.png

再加上 coulmn=”cid” cid与select cid的cid相同例如select cid as aaa那么就要写aaa

!image.png

!image.png

## mybatis缓存

!image.png

!image.png

一级缓存

!image.png

是同一个sqlseesion对象

!image.png

## 二级缓存

!image.png

，配置文件放在pojo类对应的mapper文件中，使用的pojo类实现接口

在增删改数据后也会失效

!image.png

## Ehcache 替代二级缓存

!image.png

配置方式

!image.png

!image.png

## 逆向工程

基础版本

根据数据库文件动态生成pojo，mapper文件，增删改查以及mapper接口

!image.png

!image.png

添加需要插件

!image.png

第三步

!image.png

最后在maven的插件选项中生成

!image.png

增强版本

配置方式和基础版本相同

可以创建文件Xxxexaple封装查询条件，对于pojo对象

[]()

QBC查询风格，属于增强版一种

!image.png

添加and条件

!image.png

## mybatis分页插件

!image.png

分页查询

!image.png

!image.png

pagehelper插件使用

1.引入依赖

!image.png

2.使用拦截器

!image.png

3.实现代码

!image.png

!image.png

使用startpage开启分页

!image.png

pageinfo封装分页信息对象

!image.png

注解开发

使用注解将sql列名与java属性名一一对应

!image.png

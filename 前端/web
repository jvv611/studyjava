!image.png

## HTML

超文本标记语言

<html>作为开始标签

<head>表示网页标头信息

<body>表示网页本身内容标签，网页内容在此标签中

存储在resource文件夹下

!image.png

主要的<body>的标签有如下几个

!image.png

每个标签具有属性，调整标签的位置样式

列表标签

!image.png

特殊字符

!image.png

容器的使用

!image.png

音视频的导入

!image.png

超链接

!image.png

表格属性

!image.png

from表单

!image.png

HTML `<input>` 标签的 `type` 属性决定了表单输入控件的类型。根据最新的 HTML5 标准，`type` 的取值主要可以分为以下几大类：

1. 基础/传统类

- **`text`**：单行文本输入框（如果不指定 `type` 属性，这是默认值）。
- **`password`**：密码输入框，输入的字符会被掩码（通常是星号或圆点）隐藏。
- **`radio`**：单选框（通常成组使用，`name` 属性相同为一组）。
- **`checkbox`**：复选框（多选框）。
- **`file`**：文件上传控件，允许用户选择本地文件。
- **`hidden`**：隐藏字段，页面上不显示，但其值会随表单提交。

2. 按钮类

- **`button`**：普通可点击按钮（无默认行为，需配合 JavaScript 使用）。
- **`submit`**：提交按钮，点击后会将表单数据提交到服务器。
- **`reset`**：重置按钮，点击后会将表单内所有字段恢复为默认值。
- **`image`**：图像提交按钮，使用图片作为提交按钮，点击坐标和值会随表单提交。

3. HTML5 新增 - 文本输入类

这些类型在视觉上通常与 `text` 类似，但在移动端会调出特定的虚拟键盘，且在提交表单时带有基础的格式验证。

- **`email`**：用于输入电子邮箱地址，会验证输入格式。
- **`tel`**：用于输入电话号码。
- **`url`**：用于输入网址，会验证输入是否符合 URL 格式。
- **`search`**：用于输入搜索关键词（部分浏览器会自带清除按钮）。

4. HTML5 新增 - 数值与范围类

- **`number`**：数字输入框，只允许输入数字，通常带有增减按钮。
- **`range`**：滑动条，用于输入不需要精确的数值（常配合 `min`、`max` 属性使用）。

5. HTML5 新增 - 日期与时间类

- **`date`**：日期选择器（年、月、日）。
- **`time`**：时间选择器（时、分、秒）。
- **`datetime-local`**：本地日期和时间选择器（无时区信息）。
- **`month`**：月份选择器（年、月）。
- **`week`**：周选择器（年、周）。(注：旧的 `datetime` 类型已被废弃，被 `datetime-local` 取代)*

6. HTML5 新增 - 其他特殊类

- **`color`**：颜色选择器，允许用户选择颜色，返回值为十六进制颜色码（如 `#ff0000`）。

**总结：**

如果不提供 `type` 属性，或者提供了一个浏览器不支持的值，浏览器通常会将其默认作为 **`text`** 类型处理。现代开发中推荐使用具体的语义化类型（如 `email`、`number`、`date`），因为它们能提供更好的用户体验（尤其是移动端）和内置的表单验证功能。

更改为post隐藏关键信息

!image.png

## CSS

使用style引入css格式作用

!image.png

引入方式

!image.png

选择器有标签选择器，id选择器，类选择器，通用选择器

!image.png

具体背景样式

!image.png

文本和字体样式

!image.png

## JS

定义网页的行为

基础语法

使用<script>标签在<body>底部中引入js语句

输出语句到控制台

!image.png

引入方式

行内编写

!image.png

!image.png

内部脚本

!image.png

外部脚本

!image.png

优先执行外部脚本，再内部脚本，再行内编写

变量定义

!image.png

不能i定义同一个变量定义多次，会报错

js是弱语言类型，可以在各个数据结构间相互转化，比如int变为字符串

const是常量，不能重新赋值修改数据

五种数据类型

!image.png

使用typeof（需检查对象）来看变量是什么类型

js中字符串可以使用双引号和单引号表示

最常用认识object对象

使用{}表示对象

例如

let person{

name:”张三”，

age:13，

eat:function（）{

//行为(this.属性),表示对该属性的操作

}

}

!image.png

返回e为object类型

仅只写let f

那么f会被定义为undifined

js运算符

!image.png

js只保留16位，而不会保留无限循环的数字

&和|会表示为真为1，假为0

&&以及||输出真为true，假为false

===会比较值和类型，==只比较值

表达式语句

!image.png

undifined用if判断结果为false，也就是说undifined和NaN恒为假

流程控制

wsitch以及do-while

!image.png

使用case以及条件判断控制

!image.png

for循环以及增强for循环

使用numArr定义增强for循环的标记变量

!image.png

小结

!image.png

js函数

js函数有匿名函数以及命名函数

命名函数具有参数name(params){}

在再js中不需要定义参数类型，直接定义传参名字即可

!image.png

!image.png

匿名函数

!image.png

!image.png

主要是定义的方式不同，命名函数以及匿名函数

js对象

1.Array对象，索引从0开始

!image.png

!image.png

String和自定义对象

!image.png

!image.png

## BOM

浏览器对象有BOM

其包含了window，location，tistory

!image.png

windows包含了alter，Stirng prompt以及boolean confirm

!image.png

WIN定时器

!image.png

第一个是只执行一次

第二个是周期性执行

小结

!image.png

WIN_location

写在script块中

使用地址栏对象获取网页地址栏的地址

使用href可以导航到其他地址的网页

!image.png

使用函数封装连接，有操作后跳转到对应页面

!image.png

WIN_history

!image.png

历史对象可以把握网页的前进和后退

是在body块中

!image.png

## DOM

DOM有四种对象，最重要的是Element对象

可以修改元素内容，标签以及时事件内容

!image.png

获取元素对象使用下面四种方式

id，name，tagname，classname

!image.png

可以获取html以及css的标签，形成js对象

元素事件监听

有以下事件

!image.png

设置事件的两种方式

!image.png

正则表达式

!image.png

/^表示开始，$/表示结束

!image.png

结果是不符合

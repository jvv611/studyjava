## lombok

!image.png

@Setter省略set方法，@Getter省略get方法,@ToString省略tosring方法‘

!image.png

省略hascode，全参构造，无参构造

使用@Data即可,省略全部除了构造函数外，需要配合构造函数注解使用

有些公司不让使用，因此需要使用PTG TO javabean来自动生成

## 私服

!image.png

### 私服搭建

https://developer.aliyun.com/article/1328445

!image.png

!image.png

配置启动项

配置启动端口

!image.png

在cmd中启动nexus，直接输入nexus.exe即可，进入端口8081即可

!image.png

开启步骤

!image.png

### 仓库分类

找不到的依赖回去proxy去找

snapshots正在开发中的，releases表示发布版本正式版

需要使用的时候直接使用type为group即可，会自己去找

!image.png

!image.png

### 私服配置

!image.png

替换远程仓库为国内代理

允许多次重新部署

!image.png

操作记得保存

### 工程上传

上传一般上传到relesnas或者snapshot版本中去，且需要密码

!image.png

1.设置密码

!image.png

2.设置上传位置

!image.png

pom中的id标签值和xml文件中配置id值需要对上

!image.png

3.使用项目中的deploy进行上传部署

会根据后缀上传到对应的组去，去正式组找

!image.png

!image.png

### 私服下载

添加依赖，设置下载地址在pom文件中即可

!image.png

!image.png

!image.png

## 依赖

### 依赖传递

!image.png

!image.png

面试题目

!image.png

test不会依赖junit

compile会依赖junit

provided不依赖junit

runtime依赖于junit

也就是说编译和运行时会依赖，也就是说在运行时使用的就会以来

### 依赖可选

使用配置看是否可以继续传递，b依赖a，a不想传递配置为true即可

!image.png

### 依赖排除

b依赖于a，b可以排除a中的依赖

!image.png

!image.png

### 依赖冲突

!image.png

情况一：依赖在后面的生效

情况二：直接依赖大于间接依赖，直接依赖生效

情况三：两个间接依赖在上方的生效

### 多环境开发

!image.png

!image.png

如图每个profile表示一个环境，并且每个必须有一个id，jdbc.url表示属性环境值

!image.png

!image.png

!image.png

并且jdbc需要重新配置文件为

!image.png

具有activeByDefault的表示为激活的开发环境

!image.png

然后配置资源地址

!image.png

就会将文件更改为

!image.png

根据不同的默认url地址会变换不同的mysql地址

或是在maven图形中配置

!image.png

## 继承与聚合

将各个模块分开开发，比如订单模块，用户模块等

为了不重复引入依赖使用了继承

!image.png

继承

!image.png

创建子模块方式

!image.png

父类的src就无用了可以直接删除

父类的打包和删除编译之类的会直接影响子类

### 版本锁定

父类只指定依赖版本号，不提供具体依赖

!image.png

!image.png

子模块中会有pom代码指定父类

!image.png

在父类指定使用的版本，子类使用该依赖不添加版本号时会直接使用该版本

!image.png

### 聚合

对父模块的maven操作会影响到子模块

!image.png

## 分模块开发

在一个项目中分为不同的dao包，service包和controller包来开发

!image.png

在一个父模块下开发，建立各个子模块，依赖性高

!image.png

微服务开发，各个模块尽可能独立

!image.png

这三个越往下开发越冗余，开发难，但是用户，公司友好各个模块独立性高

### 实战

!image.png

步骤

1.新建maven项目，去除src包

!image.png

2.新建子模块domain

!image.png

!image.png

3.依次创建模块

4.将使用的依赖放置到pom父依赖中去，并在子项目中使用需要的依赖

5.将pojo的依赖添加到dao的项目pom中去，否则不能使用pojo

6.添加dao的依赖到service层去，由于依赖有传递性，能直接使用pojo依赖

service层只依赖于domain或者dao层对于本体或者其他项目，最好不要依赖同级service层

!image.png

7.异常管理的实体类，放置到最底层admain层里的新建包里去

8.web层依赖于service，并转换为web项目

!image.png

所有exeception，pojo等这种实体类都放置在pojo（domain）子项目里去

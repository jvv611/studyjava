这一部分快速入门最重要

spring可以简化开发建议开发复杂度，整合了多种框架

需要了解spring framwork，spring boot，sping cloud

!image.png

## 第一阶段IOC与DI

### IOC与DI

将new对象的控制权移交到外部程序控制，解除内部的耦合，就是控制反转IOC

!image.png

Spring提供了IOC容器实现

!image.png

但是此时service的运行依赖于dao了

通过反射实现了属性的设置，以反射注入的方式称为DI，可以理解为对属性赋值，因为两个对象可能会依赖关系，需要对a进行赋值才能使用依赖a的b

由IOC创建和管理的对象叫为bean

!image.png

### 入门（绑定dao）

导入spring

!image.png

导入管理类

!image.png

IOC创建对象

!image.png

先写class，再写id，id必须唯一

此时创建好了IOC容器

使用如下代码实现调用

!image.png

### 绑定service的方法

1.去掉new

!image.png

2.提供set方法

!image.png

3.提供service和dao的bean，并且使用property属性绑定service和dao

!image.png

总结：IOC就是把对象交给spring管理，DI就是给依赖赋值，就像service对dao赋值

## IOC的Bean的配置

bean具有以下属性

!image.png

此外可以起别名（了解即可）

!image.png

name属性可以有多个别名，使用，或者空格分开

bean的作用范围

!image.png

IOC是单例模式默认，prototype是多例模式，每调用一次创建一次对象

控制工具使用单例模式合适，实体类不适用单例模式

!image.png

总共有如下的创建模式

!image.png

单例模式容器创建时候就创建对象，多例模式调用对象时候才创建

## bean的生命周期

主要包括6个

1.创建对象地址

2.调用构造方法

3.调用setter方法

4.初始化方法

5.执行调用方法

6.销毁（只有在单例模式中有用，多例模式使用JVM收回）

!image.png

service的property标签name表示标签名字，id表示需要的bean的名字，且service的方法set需要传递所依赖的id

类中添加serviceimpl中的初始化和销毁方法的方法名

!image.png

## bean创建对象的三种方式（主要是第一种）

方式一

!image.png

方式二 静态工厂方式

!image.png

!image.png

!image.png

第三种方式 实例工厂

!image.png

## 依赖注入

构造方法和set方式都可以注入简单类型和引用类型

### set方式注入

引用类型如下

!image.png

以下是简单类型

!image.png

区别在于property里是value还是ref标签

### 构造器注入（了解）

!image.png

多参数注入简单类型和引用类型

!image.png

### 自动装配

!image.png

最常用使用按引用类型注入bytype，并且需要是引用类型才能是使用自动装配

使用bytype时候保证引用类型必须只有一个，多个会报错

### 集合对象注入

需要在使用的对象里加入复杂属性

!image.png

#### 注入数组

属性名对应对象的属性，会直接调用他的set方法

!image.png

创建bean后注入如下属性

#### 注入集合

!image.png

#### 注入Set

!image.png

重复的会输出唯一

#### 注入MAP（重点）

!image.png

#### 注入properties

!image.png

## 第二阶段

## 注解开发IOC（重要）

### 管理第三方资源

#### 数据库连接池

!image.png

导入依赖，然后在xml中配置 ，上面的配置spring的bean在application-datasource.xml文件下配置，该配置是5.几版本的配置

以下是8.几的配置

!image.png

对连接进行测试

!image.png

上述方法是IOC容器方式

以下是配置properties文件配置方式

可以使用context开启命名空间

!image.png

使用${}配置读取jdbc-8.properties文件的key-value形式配置好的数据

!image.png

!image.png

!image.png

前面需要配置jdbc，否则需要配置never，否则会读取系统属性

!image.png

总结有以下配置

!image.png

测试时候需要在resource下创建所需的配置文件

!image.png

### 其他创建方式

每个不同的模块使用不同的xml配置，配置在对应路径下

!image.png

获取bean的三种方式

!image.png

最常用是感觉ID名称和类加载，找到该类的对应id的bean

!image.png

## 注解开发

使用注解@Component,自动添加到bean里去，不写注解里的名字会自动命名为相同名字的小驼峰命名

!image.png

有三个衍生注解

!image.png

开发步骤

!image.png

### 全扫描方式开发

黄色注解替代灰色部分代码

!image.png

在conig包下新建SpringConfig.java在此包中写入代码

!image.png

测试时候从注解中获取IOC容器与Bean对象方法如下

!image.png

## 注解方式下的设置

### 多例模式

在多例模式下获取多例模式方法如下

!image.png

singleton改为property即为多例模式

### bean的生命周期

标注了注解的才是初始化和销毁方法

!image.png

单例模式和多例模式前者毁掉用销毁方法，多例模式不会调用销毁方法

## 注解方式的注入

使用@Autowired进行依赖注入

!image.png

与文件配置方式一样，注解添加后也是有多个Ioc注入时候会报错，不知道依赖哪个

使用@Qualifier声明使用哪个，其中写入使用的对应的id名称

!image.png

使用@Value实现简单类型的注入

首先使用

!image.png

进行配置文件加载

然后可以使用@Value（￥{}）来进行读取配置文件的对应key的value，而不是直接写死

《容易犯错点》

bean对象创建容器时候创建

读取配置@Configuration扫描指定路径下的所有包

## 注解方式导入第三方

!image.png

首先声明出SpringConfig以及需要扫描的包注解和声明是配置类的注解

然后在该java中声明需要的第三方bean

!image.png

此外可以创建独立的类加载，如JdbcConfig

在springconfig里注解，手动导入

!image.png

对应的JdbcConfig写入上面的配置一样

但是写死了很难修改，因此使用配置文件读取的方式

使用@propertysource（“文件名”）使用@Value注解

!image.png

然后替换类里的字段

!image.png

## 第三方的bean管理

!image.png

## 对比注解和xml

!image.png

## 整合mybatis以及junit

只有整合了第三方工具后才能使用spring开发

有了配置类才能使用mybatisutil

!image.png

!image.png

都是围绕sqlSessionFactory进行的操作,所以需要对他注入

!image.png

另外map的映射的接口包也需要注入

!image.png

具体操作先添加依赖

!image.png

!image.png

创建config类

!image.png

在jdbcconfgi里注入datasource

!image.png

进行MybatisConfig的注解，先配置sqlSeessionFactory配置

!image.png

配置扫包mapper，帮助扫描包并放入容器中

!image.png

使用日志和驼峰命名

!image.png

整合junit

!image.png

使用c3p0链接池（了解）

!image.png

## 第三阶段（AOP）

在不修改原始方法的前提下增强功能，是无入侵式的编程

可以使用注解提取出反复使用的公共代码

所有方法都是一个连接点，需要增强的方法就是切入点

!image.png

### 开发案例

主要流程如下

!image.png

1.导入依赖

!image.png

2.定义连接点和实现方法以及配置springconfig

!image.png

!image.png

!image.png

4.配置增强方法，@Pointcut定义切入点

!image.png

5.绑定切入点与方法

使用@Before来定义执行切入点方法前的操作

!image.png

6.添加注解定义切面类

手动@Aspect以及@Component进行注入容器

!image.png

也可以直接在before中声明使用的方法

!image.png

7.在spingconfig1上吉祥年配置开启aop

!image.png

### AOP工作流程详解

1.启动spring容器

2.读取所有切面配置的切入点

3.初始化bean，判定bean对应的类中的方法是否匹配切入点，失败则创建对象，匹配成功则创建原始对象的代理对象

4.获取bean执行方法

!image.png

### 切入点表达式

切入点表达式有以下方式

!image.png

!image.png

!image.png

*表示至少有一个匹配，..表示可以有可以没有可以有多个参数

!image.png

!image.png

### AOP通知类型

有一下几种类型

!image.png

环绕通知包含了前面四个通知，最常用二的为环绕通知

!image.png

注意事项

!image.png

@Around方法需要使用object参数以及传递参数，因为不知道返回什么类型数据，并且使用proceed进行原操作执行

代码示例

!image.png

### AOP获取目标方法数据

可以通过传递参数获取原方法信息

!image.png

!image.png

获取数据常用环绕通知更简单

!image.png

!image.png

另外可以使用JoinPoint参数，适应他的参数获取参数，但是使用该参数需要放在参数列表的最前面，或使用ProceedingJoinPoint参数也可以获取参数

!image.png

可以修改原始参数的返回值，或是修改参数本身值有这些作用

### 获取百度网盘密码（示例）

!image.png

### spring事务简介

使用AOP实现事务的操作

!image.png

!image.png

首先添加依赖，添加mybatis涉与jdbc设置，实现账户借口以及impl代码

!image.png

!image.png

在接口上添加事务更好，可以包含更多的实现类

1.接口添加注解

!image.png

2.jdbc里添加bean，注入ioc

!image.png

3.开启事务

!image.png

现在就可以使用事务管理了

### 事务的配置

会将两个调用方法添加入开启事务的地方

!image.png

有以下注解的使用

!image.png

#### 事务传播代码示例

!image.png

由于转账的事务注解，导致该方法下的事务全回滚了，日志添加失败

需要在日志类设置

!image.png

开启了新事务

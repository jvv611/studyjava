学习1.开发步骤

2.学习配置

3.学习整合为SSM

## 入门案例

简化SpinMVC的开发流程

1.创建spring initliazer程序

!image.png

!image.png

!image.png

保留结构为

!image.png

最终结构是

!image.png

我的是

!image.png

## 官方创建方式

!image.png

开发时候必须连接网络

## 快速启动

前后端分离开发，需要连接一个数据库

!image.png

在package代码后，在此处打包

!image.png

然后将jar包给前端，前端人员在jar包目录下运行jar包即可开始运行后端代码

!image.png

想要打springbootjar包需要如下插件，包含了入口程序和后端所需要的其他依赖打包在一起

!image.png

## spring简介

!image.png

### 起步依赖

具有父工程，管理了依赖版本防止冲突

!image.png

!image.png

这样程序中已经做好了依赖，只需要加上所需要的依赖

正式由于启动项的starter依赖，其依赖了很多的开发依赖，这些依赖封装了很多的基本依赖，springboot才能以来很少启动项目，这就是起步依赖

是starter依赖一个父工程，其又依赖一个父工程，这个包含了很多的依赖固定了版本和坐标

### 辅助功能

提供了开发程序需要的功能的依赖，比如封装了tomcat

!image.png

启动方式是在@SpringBootApplication注解下的类作为程序入口

!image.png

可以使用依赖排斥替换不需要的依赖，替换为自己想要使用的依赖

这里排除了tomcat，使用了jetty服务器

!image.png

## 配置文件格式

配置端口

!image.png

在applicatipn。properties文件下更改

!image.png

或是新建application.yml

数字值和配置英文冒号后需要有空格

!image.png

或是使用yaml文件

!image.png

在文件不出提示的时候需要在模块中更改，将文件添加到配置文件目录中去

!image.png

!image.png

!image.png

以后主要是写yml格式的配置文件，这三种文件优先级别为properties>yml>yaml

### yaml格式

近几年开始占据主导位置

!image.png

yaml的语法规则

!image.png

数组格式为

!image.png

### 读取yaml格式

方式一

读取属性直接注入读取对应值即可

!image.png

读取数组数据

!image.png

!image.png

方式二

简化的读取方式使用环境变量读取，使用自动装配会读取配置文件里的所有信息封装在enviroment对象里去

!image.png

读取方式如下

!image.png

!image.png

方式三

对象方式读取yaml中的数组

定义对象为bean，并且表示为丛配置中读取

!image.png

然后自动装配该对象

!image.png

读取信息

!image.png

!image.png

通常是用来配置第三方工具

有时候自定义对象封装会报错需要添加依赖

!image.png

## 多环境开发

!image.png

在yml文件中配置三种环境即可，yml使用—-区分环境，并设置使用的环境

!image.png

不过时写法如下

!image.png

properties格式应该是多文件选择配置环境

配置主环境文件，选择读取哪个文件

!image.png

被读取文件再配置信息

!image.png

## 多环境开命令行启动参数格式

必须文件无乱码打包才不会影响后续使用

打包完开始运行

!image.png

需要更改文件代码为，按照需要更改

!image.png

## 多环境开发兼容问题

maven的profile优先级应该大于spring的profile来指导spring的使用

在pom中设置的启动环境小于spring的application.yml的启动环境配置

!image.png

!image.png

为了使读取pom的配置环境需要配置pom文件为如下

!image.png

并且配饰spring文件为如下，解析pom文件

!image.png

为了能够处理资源文件需要加上插件，这样pom文件可以影响到application.yml文件

!image.png

## 配置文件分类

简化命令的配置，不需要使用很长的代码启动

!image.png

config文件夹下的配置文件将会覆盖外层的配置文件，这样在开发时候删除config中文件开发配置文件，上限再把外围文件复制到config中即可

!image.png

那么为了减少打包的jar包启动命令过长，可以在ja包目录下将开发的配置文件复制过去，上线需要修改配置文件也可以使用这个方式，并且也可以再config中覆盖外围的配置文件同样

!image.png

## springboot整合第三方方法

### 整合Junit

spring的整合方法为

!image.png

springboot整合为

实现了功能后进行测试，直接使用@Test注解即可

!image.png

sprinbootconfig文件的包及其子包下的所有数据会被扫描，再SpringBootApplication.java文件中

### 整个Mybatis

springboot自然整合了mvc以及spring，只需要整合mybatis

!image.png

spring配置方式

!image.png

springboot配置方式

配置项目时候

!image.png

添加依赖

!image.png

再pom文件中会自动添加所需要的依赖

添加实体类，dao层查询数据配置完

需要在dao层里配置@Mapper，让他知道是自动装配需要

!image.png

还需要配置环境

!image.png

此时就整合完毕了

额外可以添加连接池

!image.png

再环境中配置

!image.png

### 一个案例

!image.png

新项目

!image.png

添加起步依赖

!image.png

添加druid依赖

再yml中配置数据库，tomcat等配置

!image.png

添加测试类，看目前的配置是否正确

接下来配置前端创建资源包下的satic来放置前端页面

!image.png

配置主页，直接跳转主页

!image.png

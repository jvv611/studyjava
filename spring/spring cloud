## 微服务结构演变

在代码量太大时候，模块可能有上百的时候，按照业务拆分未独立模块，降低耦合度

!image.png

为了保证高可用还需要集群，远程调用

!image.png

微服务是一种分布式方案

!image.png

## 微服务结构

!image.png

技术上的实现差别

!image.png

企业需求的组合可能是如下，第四种可以直接升级为3，只需要更改一些配置

!image.png

## Spring Cloud

可以访问官网，springcloud整合了很多公司的三方工具，并且基于自动装配使用，因此火了起来

!image.png

具有版本的注意事项，防止版本冲突

!image.png

### 拆分案例

!image.png

!image.png

选择show展示所有微服务结构

!image.png

想要查询订单的同时查询用户信息需要引入远程调用

### 微服务远程调用

!image.png

所以需要java程序来发送请求，模拟像http一样发送请求

!image.png

在此处注入resttrmpalte

!image.png

2.写出查询的controller，注意区分get请求未getforobjet，post未postfotobject

具体如下

告诉请求的路径和返回所需要的类型

!image.png

## Eureka

服务的消费者和请求者

!image.png

a调用b，b调用c时候，b相对于a为提供者，相对于c为消费者

### Eureka中心原理

之前的服务调用是硬编码服务地址，需要更改每一次改代码，且可能有多个服务器地址需要请求

!image.png

使用eureka来解决，需要eureka服务的为eureka的客户端

记录了所有服务的地址，需要使用的时候请求出来，并使用负载均衡提供看去哪个服务消费

30s请求一次看服务提供者的状态，没发送就是故障了会剔除出去从注册队列中

!image.png

!image.png

### 搭建eureka

#### 创建eureka服务

搭建步骤

!image.png

1.引入依赖，并写出配置文件

只需要写所需的依赖，父工程已经制定了版本

!image.png

1.具体是添加完依赖后

2.写出设置类

!image.png

3.编写配置类 注意eureka需要自己配置自己的信息，因为可能存在eureka集群

!image.png

运行后会有eureka的配置，点击后到达eureka的配置信息网页，最主要展示了eureka的实例信息

!image.png

!image.png

#### 实例注册eureka服务

步骤如下

!image.png

具体是

1.添加依赖，在需要管理的client中，注意是引入client依赖不是server依赖

!image.png

2.配置yml文件

配置服务名称和eureka的服务地址

!image.png

!image.png

测试搭建多个实例

使用快捷键controll + d

!image.png

会有两个实例创建就会

!image.png

### eureka服务发现

!image.png

eureka根据服务名获取服务列表，写入服务名即可来获取读物列表

1.写入服务名称，在所需要远程调用的地方

!image.png

2.加上注解使用负载均衡

!image.png

## 负载均衡

### 原理

负载均衡使用Ribbon实现

!image.png

拦截到uri后获取信息并发送给eureka

!image.png

获取到了服务列表

!image.png

接下来负载均衡

!image.png

有个chooseserver方法进入后为调用了IRule的接口的实现类的方法

!image.png

### 负载均衡的实现接口

!image.png

有常见的轮询规则为，默认为领域轮询的方法

!image.png

修改负载均衡策略的方法如下

!image.png

具体为在配置类中添加注解，并且选择方法

!image.png

或者是在需要更改模块中的yml配置文件中更改负载均衡规则

!image.png

## Ribbon策略饥饿加载

第一次请求为500ms

!image.png

第二次为27ms

!image.png

因为ribbon为懒加载，创建过程中才拉去服务去创建需要的模块类如orderservice

!image.png

可以配置饥饿加载，在yml的配置文件中

!image.png

需要多个的时候配置数组在yml文件中

!image.png

此时是初始化时候就加载，类似的有dispatechServlet也是懒加载，创建实例的时候才加载

## Nacos

### 安装

nacos和eureka一样也是注册中心，但是相比eureka功能更加丰富

安装方法在学习的文件夹中

启动命令如下

D:\nacos\nacos-server-2.1.0\nacos\bin>startup.cmd -m standalone

账号密码均为nacos

### 快速入门

1.更改需要nacos的模块的依赖，引入alibaba的父工程进行版本管理

!image.png

2.配置需要使用nacos的模块进行配置yml文件，在spring的配置中配置

!image.png

### nacos服务分级存储模型

将一个服务的多个实例放置到不同地方去，组件集群

!image.png

nacos划分了地区的结构相比于eureka更加细节，主要是防止跨集群访问

!image.png

配置方法如下

!image.png

具体是

1.添加yml配置

!image.png

2.启动需要在hz的实例

3.更改为sh

!image.png

4.启动需要在sh的实例

### nacos的负载均衡

修改负载均衡的方法只需要修改yml文件配置

!image.png

这个方法是优先访问本地集群，本地集群内是随机访问，跨集群访问会警告

!image.png

### 服务实例的权重设置

设置好设备更多请求，设备差更少请求

可以直接在注册中心更改，权重高请求频率约高，权重为0时不会被访问

!image.png

权重为0方便了服务的升级，多个服务器轮流为0轮流升级，并且升级完后可以设置小权重进行测试

### nacos环境隔离

namespace对服务进行隔离，或是对数据做一些隔离

!image.png

namespace主要是用来划分测试环境，开发环境，上线环境

在nacos的页面里有命名空间，可以新建，默认为public空间

!image.png

!image.png

需要在命名空间dev中添加

添加位置是在yml文件更改空间

!image.png

跨空间是无法访问的，碧玺相同环境才能访问

## 对比euraka和nacos

不会每次都拉取，会缓存在列表缓存，有就不拉取，30s更新一次缓存

nacos会把实例划分为临时实例和非临时实例，临时实例使用心跳检测，未检测到会直接剔除

非临时实例不要求做心跳，而是nacos主动询问非临时实例是否还在，不在的时候不会剔除而是标记不健康

nacos有服务变更后，会主动告知消费者信息

!image.png

配置非临时实例方法如下

!image.png

## nacos配置管理

### 统一配置管理

希望可以更改可以改一处，其他地方都自己更改了，且是热更改

利用微服务定时读取配置的特性

!image.png

实现配置管理的方式如下，在配置列表中点击 + 添加配置

!image.png

进行配置

!image.png

添加需要热更新的配置，将其添加到配置中

!image.png

然后发布即可

### 微服务读取配置

需要将读取文件流程更改为

!image.png

利用bootstrap.yml来实现，因为在项目启动后最先读取该文件

步骤如下，注意服务名称和之前创建的名称对应上，开发环境的名字也要对应上

!image.png

1.写入依赖

2.新建bootstrap.yml文件，并配置

!image.png

3.去除重复的application.yml的配置

4.测试

!image.png

### 热更新

方式一

!image.png

在controller类下注解，需要的controller进行注解

!image.png

方式二

!image.png

在配置类中，对需要热更新的进行注解

!image.png

## 多环境配置共享

不管环境怎么变化，都可以共享配置 只需要nacos添加环境配置的时候写公共的名字

!image.png

userservice会被所有userservice的name配置的bootsreap.yml文件读取，dev会被其profile下的active为dev的读取

!image.png

首先需要读取新建的配置文件 此处为env那个，看是否读取上

!image.png

当环境配置中与本地bootstrp.yml冲突时，环境配置优先级别高，且当前环境配置大于共享环境配置

## 企业级nacos集群搭建

实现如下结构

!image.png

步骤为

!image.png

nacos集群搭建.md

具体流程在这个md文件中，其中也包含了nginx的使用，并且将配置的入口改为nginx端口

!image.png

## Feign远程调用

!image.png

!image.png

步骤如下

!image.png

!image.png

这样只对该服务有效

具体如下

1.添加依赖

!image.png

1. 开启自动装配声明@EnableFeignClients

!image.png

3.写客户端声明，服务名称，请求方式，传递参数之类的请求信息

!image.png

4.使用客户端，切除掉restTemplate代码

5.注入service，将客户端

!image.png

fegin自动有负载均衡，已经集成了rebbion，会负载均衡调用远程方法

### feign的自定义配置

有以下的默认配置可更改，但不是全部可修改的配置

!image.png

fegin集成了ribbon，实现了失败重试机制，一般修改配置就修改日志级别

方式一 application.yml文件修改

!image.png

方式二 java代码修改

!image.png

例如下列为全局有效，配置在启动类中

!image.png

加载此处Client上是局部有效

!image.png

### 性能优化

!image.png

需要使用连接池强化性能

步骤如下

!image.png

步骤一

引入依赖

!image.png

步骤2

配置yml文件

!image.png

### fegin的最佳实践

传递的请求和实际服务的请求需要一样才能请求到

!image.png

方式一：通过继承实现，fegin和controller统一接口，但是会导致紧耦合

!image.png

方式二 抽取出来

!image.png

但是会在service里引入冗余的多种方法

方式二的实现步骤

步骤如下

!image.png

具体如下

1.新建模块，并且引入依赖

!image.png

2.将有关的pojo，配置类和对应的Client接口导入到配置中

!image.png

3.在被删除了pojo等的模块中添加fegin模块的依赖

!image.png

4.修改代码错误处即可

5.添加扫描包，在之前的被删除pojo等的application.java中添加注解指定扫描包

!image.png

!image.png

## 网关

### 了解网关

!image.png

可以利用下列方式实现网关

!image.png

### 搭建网关方式

1. 新建模块，并且添加依赖、

!image.png

配置完为

!image.png

2.通过配置实现网关路由

!image.png

在该模块中配置application.yml 文件

!image.png

### 路由断言工厂

断言是由断言工厂来解决

!image.png

有十几种断言工厂，来解决不同的断言

!image.png

只需要配置在文件中 例如

!image.png

### 路由过滤器配置

对请求和响应进行处理后再通过到下一处

!image.png

直接在官网中查看需要的过滤器和作用即可

!image.png

添加方式如下 直接写在网关配置文件中

!image.png

!image.png

如果需要在所有服务中都配置过滤器，可以使用全局过滤器

default-filter

!image.png

!image.png

### 全局过滤器

之前的实现方式是配置实现，有些复杂的业务逻辑是需要自定义的，所以需要全局过滤器

!image.png

exchange为获取的信息，可以在整个过滤器传递

chain是表示过滤器链

案例

!image.png

在网关的模块下写过滤器

!image.png

登录过滤器的过滤逻辑

!image.png

并在类头部定义过滤器执行顺序和注入容器，@Oreder越小优先级越高

!image.png

也可以使用接口实现

!image.png

重写方法

!image.png

### 过滤器执行顺序

!image.png

配置文件中的过滤器执行顺序由spring来指定执行顺序

!image.png

!image.png

### 网关跨域问题处理

浏览器禁止ajax请求

!image.png

只需要在网关中配置

!image.png

配置网关的application.yml文件添加

!image.png

# 第一阶段

复习

!image.png

!image.png

开启事务操作

现在事务注解写在方法上即可

!image.png

!image.png

事务传播属性设置

!image.png

!image.png

主要关注前面三个和最后一个

## 入门简介

!image.png

springmvc是一个轻量级的web框架，使用注解代替servlet开发，支持restful风格开发

## 入门案例

!image.png

1.添加依赖

!image.png

2.创建核心配置类

!image.png

3.并且配置返回视图的前缀和后缀

!image.png

4.配置ServletConfig，初始化并配置servlet，放入ioc中

!image.png

5.自定义controller类，实现表现层

!image.png

此时返回sucess，到达springmvcconfig层后会拼接为/pages/success.jsp去找对应文件因此需要下一步

6.常见pages目录以及success.jsp文件

!image.png

7.web项目添加tomcat

@RequestMapping返回的是路径，并且访问jsp文件，返回视图前端页面

@ResponseBody表示访问的是返回的字符串而不是视图jsp文件，返回JSON字符串

小结

!image.png

### 入门案例分析

!image.png

!image.png

## postman的使用解析请求与响应，调试请求

postman用来测试接口

1.点击加号创建请求

!image.png

2.将服务器地址访问资源地址写入

!image.png

!image.png

3.若测试时间很久需要保存可以点击save进行保存

!image.png

!image.png

!image.png

## 请求映射路径

!image.png

在具有统一的前半路径时候可以在类上声明@RequestMapping（“/统一路径”）

会在所有方法上配置该路径

!image.png

## get与post请求传参

get的发送

!image.png

也就是传参name与url的传参name相同名字

post的发送

!image.png

也是需要参数名字相同

如果出现了post请求的乱码，需要在servletconfig配置，重写过滤器方法

!image.png

## 请求参数起别名

有以下类型的参数

!image.png

不想暴露参数名字以及对其前后端参数名字可以使用别名

!image.png

调试的时候需要先调试有没有进入到该方法再去调试其他service层和mapper层

## 传递实体参数

导入实体对象pojo类

这里导入了user，address，book

!image.png

重写了tostring后可以直接输出对象，使用uer对象传递参数

!image.png

这里对象的属性名和postman形参名字要相同，才能解收到值的传递

参数为User，所以会封装为user对象

### 嵌套传递POJO参数

!image.png

在posyman中需要使用.来找到属性转换为user对象封装

!image.png

### 集合和数组参数的传递

传入数组

!image.png

首先传入的参数与postman的形参需要对应上，才能传递数组

其次，需要参数名字相同在postman中直接就是数组了

传入集合

添加@RequestParam进行表示，自动转换为集合

!image.png

### 接受JSON格式数据

可能有以下三种格式

!image.png

需要引入json的依赖

!image.png

#### 获取json

1.引入json依赖

!image.png

2.接受json数据

!image.png

!image.png

3.开启json数据格式自动转换

!image.png

!image.png

4.发送json数据，使用post发送json，也可以get发送但是一般使用post发送

发送方法如下

!image.png

#### json获取对象和数组对象

使用类嵌套传递普通数组类型，可以进行嵌套

!image.png

添加接受方法，在最后一个

!image.png

传递json格式数据，注意json的写法格式

!image.png

可以进行嵌套

!image.png

**传递json数组**

!image.png

!image.png

### 接受日期类型

默认是/隔断类型（yyyy/mm/dd），其他类型需要自定义使用@DateTimeFormat注解进行替换，记得月要大写

!image.png

接受

!image.png

传递

!image.png

## 响应Json，字符，对象，数组

响应文本

!image.png

响应json数据

!image.png

!image.png

响应集合

!image.png

!image.png

!image.png

响应朱慧响应页面以及json格式的数据

## RESTful风格开发

根据不同的请求方式使用不同的开发，一般传递json数据

!image.png

入门案例

value表示路径，method表示请求方式

修改和存储

!image.png

路径可以一致，但是请求方式需要不同

获取和删除

使用@PathVariable配对参数与路径中的参数

!image.png

!image.png

!image.png

可以把方法上的@ResponseBody同意放在类上

对应类可以更改为xxxMapping

例如@PostMapping，@DeleteMapping，@DeleteMapping("/{id}")等来表示资源的路径

!image.png

# 第二阶段

## 放行静态资源

restful的开发不适用requestful而是使用getmapping，deletmapping等

要存储前端数据需要放到webapp里去新建的webapp需要删除原来的target目录，重新编译

默认访问url是访问动态资源，会去访问对应的路径入loclahost：80/pages/main.html而不是去访问静态资源，因此需要放行静态资源

方法1

!image.png

推荐使用方法2

在springmvcconfig里新增方法，回去访问pages目录下的静态资源

!image.png

/pages/*表示目录下的子资源,/pages/**表示访问pages下的所有资源

## 拦截器（拦截动态资源）

整合spring mvc spring mybstis为SSM

!image.png

!image.png

过滤器使用servlet实现，拦截器使用Springmvc动态控制，反射实现

!image.png

1.实现拦截器

!image.png

2.定义配置类，看拦截哪个controller

!image.png

标准化开发使用

!image.png

## 开发方式

1.maven转为web项目，并且配置依赖

!image.png

2.配置config

!image.png

3.实现拦截器

!image.png

4.定义拦截器，添加那个拦截器，拦截谁

!image.png

## 拦截器的执行顺序

!image.png

## 拦截器参数

!image.png

后置的参数

!image.png

最终拦截的参数

!image.png

## SSM项目的创建

主要是整合依赖和数据

!image.png

步骤如下

1.创建maven项目

2.转换为webapp项目

3.添加依赖

!image.png

4.配置核心配置类

!image.png

!image.png

5.配置数据库

5.1配置数据源

!image.png

5.2配置jdbcconfig

导入文件

!image.png

在jdbcconfig配置

!image.png

导入核心配置中使用@Import

!image.png

6.配置mybatis

添加依赖

!image.png

配置mybatisconfig，映射配置与数据源配置

!image.png

!image.png

添加到核心配置类中@Import去

7.开启事务

在jdbcconfig中添加如下字段

!image.png

!image.png

在核心配置上开启事务

!image.png

在service层上开启@Transaction注解即可

8.开启JSON格式的相应获取

添加依赖以及在核心配置类开启功能

!image.png

!image.png

9.整合springmvc

方法一

!image.png

第二种配置方式

!image.png

!image.png

!image.png

之后导入数据进入数据库

进行测试时候需要导入junit

## Dao层的实现

1.创建pojo对象

2.创建mapper层的pojo对象的Dao接口

!image.png

3.创建mybatis映射文件或注解开发

!image.png

## Controller编写

!image.png

## 统一返回结果对象

更改为右侧这样

!image.png

!image.png

需要具有状态码看结果状态，以及数据和信息

!image.png

状态码接口

!image.png

result实体类

!image.png

接下来修改控制层的返回结果，返回result类型，并且返回对应的信息

!image.png

## 异常处理器

错误的时候也按照result的code的格式返回错误信息

!image.png

实现是

!image.png

具体实现

!image.png

不需要写出真实的错误

!image.png

### 项目异常处理

!image.png

系统异常

!image.png

!image.png

在通知中导入需要的信息

!image.png

业务异常

!image.png

!image.png

添加到通知中

!image.png

!image.png

复杂代码都是卸载service层
